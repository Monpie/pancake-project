{% extends '::base.html.twig' %}

{% block navbar %}

    {# block header #}
    {% block header %}
        <img class="d-block " src="/bundles/images/logo.jpg"  alt="Logo">
    {% endblock %} {# fin block header #}

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark" text-align="right">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav">


                <li class="nav-item active" >
                    <a class="nav-link" href="{{path('home')}}">Accueil <span class="sr-only">(current)</span></a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Catalogue
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                        <a class="dropdown-item" href="{{path('crepes')}}">Crêpes</a>
                        <a class="dropdown-item" href="{{ path('pancakes') }}">Pancakes</a> {#Modifier le href quand le fichier sera créé #}
                        <a class="dropdown-item" href="{{path('newCrepe')}}">Ajouter Crêpe</a>
                    </div>
                </li>


                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{path('panier')}}">Panier</a>
                </li>
                {% if  app.user != null%}
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('logout') }}">Déconnexion</a>
                </li>
                     <li class="nav-item">
                    <a class="nav-link" href="{{ path('editUser',{'id' : app.user.id}) }}">Paramètres</a>
                     </li>
                {% else %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Connexion
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="{{path('login')}}">Connexion</a>
                            <a class="dropdown-item" href="{{path('newUser')}}">Inscription</a>
                    </div>
                </li>
                {% endif %}

            </ul>
        </div>
    </nav>
{% endblock %} {# fin block navbar #}